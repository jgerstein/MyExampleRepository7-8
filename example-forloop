int step = 1;

void setup() {
  size(800, 600);
}

void draw() {
  background(255);
  step = mouseX + 1;
  for (int x = 0; x <= width; x += step) {
    line(x, 0, x, height);
  }
}

/*
for(initialize; test; increment){
 do a thing
 }
 */
